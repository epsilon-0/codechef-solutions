#include<cstdio>

using namespace std;

int nums[100005];

int main(){
  int t, n, k;
  scanf("%d", &t);
  while(t--){
    scanf("%d %d", &n, &k);
    for(int i = 0; i < k; i++){
      scanf("%d", nums+i);
    }
  }
}
